1. Did it go as planned? Mostly -- the regrouping and fold work completed as planned, but the export-count target was not fully met.
2. What changed from the parent issue plan: Sub-issues #48-#51 landed the planned manifest/apply/fold sequence and preserved CLI behavior, but final public export count settled at 32 rather than the target <=30. Per parent flag policy, this is recorded as a metric renegotiation: the remaining two exports are intentionally retained because their removal would require additional API redesign beyond this cycle's behavior-preserving refactor scope.
3. ADRs made during this parent issue (reference INDEX.md rows): None. ADR 0005 was not created; the final grouping rationale remained straightforward and is documented here and in the cycle PRD update.
4. New considerations or constraints surfaced: Repository-runnable verification remains the practical closure gate for this cycle (`pnpm test`, engine build with DTS), while the standalone no-emit TypeScript command noted in sub-issue #49 was not a reliable workspace gate.
5. Patterns across sub-issue AARs: Decision-first decomposition worked: #48 established explicit KEEP/FOLD/HIDE intent, #49 applied mechanically, and #50/#51 isolated deeper API folds without mixing behavior change into regrouping.
6. Carry-forward -- flags to write in the cycle, notes for the PRD AAR: Cycle close should record that domain-led public grouping is now settled (Periodization, Runs and capture, Metrics and zones, Analysis output, Infrastructure) and that export-count success moved from hard target <=30 to achieved 32 with explicit rationale rather than silent drift.
