{
  "name": "run2max",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "dev": "pnpm -r run dev"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "vitest": "^4.1.5"
  }
}
